# Only needed for production builds where the API is on a different origin
# than the frontend. Leave unset for local dev — Vite's dev server proxies
# /api to the local backend (see vite.config.ts).
VITE_API_URL=
